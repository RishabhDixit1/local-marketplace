export const appName = "ServiQ";
export const appTagline = "Connecting people with Human-Centered Services Near You!";

export const appDescription =
  "ServiQ is a realtime local marketplace for trusted, human-centered services and products.";

export const withAppName = (value: string): string => `${value} | ${appName}`;
